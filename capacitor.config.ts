import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.saludia.meditranslate',
  appName: 'MediTranslate',
  webDir: 'dist/public'
};

export default config;
